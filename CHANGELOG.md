# Changelog

Ad Namebuilder 的改动记录。

## 版本号规则

| 号段 | 含义 |
|---|---|
| `v1.0` | 上线基线 |
| `v1.1`、`v1.2`… | 功能或规则调整，每次 +0.1。**不改变已有命名的生成口径**，旧命名依然有效 |
| `v2.0` | **命名结构变更**：增删字段、调整段位顺序、修改连接符规则。此时旧命名规则作废 |

> 说明：仓库早期的 `v5` / `v7` / `v10` 属于开发迭代号，与当前 `v1.x` 发布号不是同一套体系，不要混用。

---

## v1.1 · 2026-09-23

### Custom 字段放开百分号 `%`

**背景**：Ad 层的 Custom 字段需要支持折扣类命名（如 `20%off`、`spend-200-save-40%`），但字符过滤是**去空格后转连字符，再剔除白名单外字符**，`%` 不在白名单内，一输入就被吃掉。

**改动范围**：Ad Campaign / Ad Group / Ad 三层 Custom 输入框

**具体内容**
1. 字符白名单由 `a-z 0-9 - +` 扩展为 `a-z 0-9 - + %`
2. 修复 `%` 相邻空格产生多余连字符的问题，并固定处理顺序为：
   - ① 先消掉紧邻 `%` 的空格
   - ② 再把剩余空格 / 下划线转成连字符
   - ③ 最后剔除白名单外的字符
3. 三处输入框的提示文案与规则说明同步更新
4. 页头新增版本号显示

**行为对照**

| 输入 | v1.0 | v1.1 |
|---|---|---|
| `50 % off` | `50-%-off` | `50%off` |
| `20% off` | `20%-off` | `20%off` |
| `Save 50 %  OFF` | `save-50--%-off` | `save-50%off` |
| `spend $200 save 40%` | `spend-200-save-40` | `spend-200-save-40%` |
| `Bogo_50%` | `bogo-50` | `bogo-50%` |
| `100%+extra` | `100+extra` | `100%+extra` |
| `reolink  #1  brand` | `reolink-1-brand` | `reolink-1-brand`（不变） |

**影响范围**
- 仅前端字符过滤逻辑，`data.json` / `product-code-map.json` / `custom-options.json` 均无变化
- 不改动已有命名口径，非 `%` 场景行为完全一致
- `&` `$` `#` 及中文等仍按原规则被过滤
